# =====================================================================
# Proyecto TeleSecure — docker-compose.yml  (VERSION VM LINUX)
# Ajustado para network_mode: host en FreePBX -> el audio RTP funciona.
#
# Cambios respecto a la version Windows:
#   - FreePBX usa network_mode: host (sin ports ni networks propias)
#   - FreePBX se conecta a la BD por 127.0.0.1 (DBHOST), via puerto publicado
#   - La BD publica 3306 al host para que FreePBX la alcance
#   - El webhook apunta a FreePBX por la IP de la VM
# =====================================================================

services:

  # -------------------------------------------------------------------
  # 1) BASE DE DATOS CENTRAL — MariaDB 10.6
  # -------------------------------------------------------------------
  db:
    image: mariadb:10.6
    container_name: telesecure_db
    restart: unless-stopped
    environment:
      MARIADB_ROOT_PASSWORD: ${DB_ROOT_PASSWORD:-RootCambiar123!}
      TZ: America/Lima
    command:
      - --character-set-server=utf8mb4
      - --collation-server=utf8mb4_unicode_ci
      - --sql-mode=
    volumes:
      - db_data:/var/lib/mysql
      - ./db/init.sql:/docker-entrypoint-initdb.d/01-init.sql:ro
    networks:
      - red_datos
    ports:
      - "127.0.0.1:3306:3306"           # publicado SOLO a localhost (FreePBX host-net lo alcanza)
    healthcheck:
      test: ["CMD", "healthcheck.sh", "--connect", "--innodb_initialized"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

  # -------------------------------------------------------------------
  # 2a) PostgreSQL exclusivo de midPoint
  # -------------------------------------------------------------------
  midpoint_data:
    image: postgres:16-alpine
    container_name: telesecure_midpoint_data
    restart: unless-stopped
    environment:
      POSTGRES_DB: midpoint
      POSTGRES_USER: midpoint
      POSTGRES_PASSWORD: ${MIDPOINT_DB_PASSWORD:-MidpointCambiar123!}
      TZ: America/Lima
    volumes:
      - midpoint_pgdata:/var/lib/postgresql/data
    networks:
      - red_datos
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U midpoint -d midpoint"]
      interval: 10s
      timeout: 5s
      retries: 5

  # -------------------------------------------------------------------
  # 2b) Inicializador del repositorio de midPoint
  # -------------------------------------------------------------------
  midpoint_init:
    image: evolveum/midpoint:latest-alpine
    container_name: telesecure_midpoint_init
    depends_on:
      midpoint_data:
        condition: service_healthy
    command: >
      bash -c "
      cd /opt/midpoint ;
      bin/midpoint.sh init-native ;
      echo ' - - - - - - ' ;
      bin/ninja.sh -B info >/dev/null 2>/tmp/ninja.log ;
      grep -q 'ERROR' /tmp/ninja.log && (
      bin/ninja.sh run-sql --create --mode REPOSITORY ;
      bin/ninja.sh run-sql --create --mode AUDIT
      ) ||
      echo 'Repositorio ya inicializado, nada que hacer' ;
      "
    environment:
      - MP_SET_midpoint_repository_jdbcUsername=midpoint
      - MP_SET_midpoint_repository_jdbcPassword=${MIDPOINT_DB_PASSWORD:-MidpointCambiar123!}
      - MP_SET_midpoint_repository_jdbcUrl=jdbc:postgresql://midpoint_data:5432/midpoint
      - MP_SET_midpoint_repository_database=postgresql
      - MP_INIT_CFG=/opt/midpoint/var
    networks:
      - red_datos

  # -------------------------------------------------------------------
  # 2c) midPoint (IAM)
  # -------------------------------------------------------------------
  midpoint:
    image: evolveum/midpoint:latest-alpine
    container_name: telesecure_midpoint
    restart: unless-stopped
    depends_on:
      midpoint_data:
        condition: service_healthy
      midpoint_init:
        condition: service_completed_successfully
    command: [ "/opt/midpoint/bin/midpoint.sh", "container" ]
    environment:
      - MP_SET_midpoint_repository_jdbcUsername=midpoint
      - MP_SET_midpoint_repository_jdbcPassword=${MIDPOINT_DB_PASSWORD:-MidpointCambiar123!}
      - MP_SET_midpoint_repository_jdbcUrl=jdbc:postgresql://midpoint_data:5432/midpoint
      - MP_SET_midpoint_repository_database=postgresql
      - MP_SET_midpoint_administrator_initialPassword=Telesecure5ecr3t
      - MP_UNSET_midpoint_repository_hibernateHbm2ddl=1
      - MP_NO_ENV_COMPAT=1
      - TZ=America/Lima
    ports:
      - "8081:8080"
    volumes:
      - midpoint_home_v4:/opt/midpoint/var
    networks:
      - red_datos
      - red_gestion

  # -------------------------------------------------------------------
  # 3) FreePBX — network_mode: host (CLAVE para el audio RTP)
  # -------------------------------------------------------------------
  freepbx:
    image: blacksunsolutions/freepbx:latest
    container_name: telesecure_freepbx
    restart: unless-stopped
    network_mode: host                  # <<<<<< comparte la red de la VM directamente
    cap_add:
      - SYS_NICE
      - NET_ADMIN
    depends_on:
      db:
        condition: service_healthy
    environment:
      TZ: America/Lima
      DBENGINE: mysql
      DBHOST: 127.0.0.1                 # <<<<<< la BD se alcanza por localhost (puerto publicado)
      DBPORT: "3306"
      DBNAME: asterisk
      CDRDBNAME: asteriskcdrdb
      DBUSER: ${FREEPBX_DB_USER:-fpbx_user}
      DBPASS: ${FREEPBX_DB_PASSWORD:-FreepbxCambiar123!}
      DB_USER: ${FREEPBX_DB_USER:-fpbx_user}
      DB_PASS: ${FREEPBX_DB_PASSWORD:-FreepbxCambiar123!}
      USER: asterisk
      GROUP: asterisk
      WEBROOT: /var/www/html
      ASTETCDIR: /etc/asterisk
      ASTMODDIR: /usr/lib64/asterisk/modules
      ASTVARLIBDIR: /var/lib/asterisk
      ASTAGIDIR: /var/lib/asterisk/agi-bin
      ASTSPOOLDIR: /var/spool/asterisk
      ASTRUNDIR: /var/run/asterisk
      AMPBIN: /var/lib/asterisk/bin
      AMPSBIN: /usr/sbin
      AMPCGIBIN: /var/www/cgi-bin
      AMPPLAYBACK: /var/lib/asterisk/playback
    # SIN 'ports:' — host networking expone los puertos directo en la VM
    # SIN 'networks:' — comparte la red del host
    volumes:
      - freepbx_varlib:/var/lib/asterisk
      - freepbx_etc:/etc/asterisk
      - freepbx_usrlib:/usr/lib64/asterisk
      - freepbx_logs:/var/log/asterisk
      - freepbx_www:/var/www/html
    healthcheck:
      test: ["CMD-SHELL", "fwconsole --version > /dev/null 2>&1 || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 10
      start_period: 300s

  # -------------------------------------------------------------------
  # 4) WEBHOOK — Conector midPoint -> FreePBX
  # -------------------------------------------------------------------
  webhook:
    build:
      context: ./webhook
    container_name: telesecure_webhook
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
      freepbx:
        condition: service_started
    environment:
      DB_HOST: db
      DB_PORT: "3306"
      WEBHOOK_DB_USER: ${MIDPOINT_DB_USER:-mp_user}
      WEBHOOK_DB_PASSWORD: ${MIDPOINT_DB_PASSWORD:-MidpointCambiar123!}
      WEBHOOK_TOKEN: ${WEBHOOK_TOKEN:-CAMBIAR_TOKEN_WEBHOOK}
      FREEPBX_CONTAINER: telesecure_freepbx
      # FreePBX esta en host networking -> se alcanza por la IP de la VM
      FREEPBX_API_BASE: ${FREEPBX_API_BASE}
      FREEPBX_CLIENT_ID: ${FREEPBX_CLIENT_ID:-}
      FREEPBX_CLIENT_SECRET: ${FREEPBX_CLIENT_SECRET:-}
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    networks:
      - red_datos
      - red_gestion
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request as u; u.urlopen('http://localhost:5000/health')\""]
      interval: 30s
      timeout: 5s
      retries: 5

# =====================================================================
# REDES SEGMENTADAS
# =====================================================================
networks:
  red_datos:
    driver: bridge
    name: telesecure_red_datos
  red_voz:
    driver: bridge
    name: telesecure_red_voz
  red_gestion:
    driver: bridge
    name: telesecure_red_gestion

# =====================================================================
# VOLÚMENES PERSISTENTES
# =====================================================================
volumes:
  db_data:
    name: telesecure_db_data
  midpoint_home_v4:
    name: telesecure_midpoint_home_v4
  midpoint_pgdata:
    name: telesecure_midpoint_pgdata
  freepbx_varlib:
    name: telesecure_freepbx_varlib
  freepbx_etc:
    name: telesecure_freepbx_etc
  freepbx_usrlib:
    name: telesecure_freepbx_usrlib
  freepbx_www:
    name: telesecure_freepbx_www
  freepbx_logs:
    name: telesecure_freepbx_logs
